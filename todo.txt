For conan:

pip install conan

conan profile detect --force # if your profile is not already created

conan install . --output-folder=build --build=missing

For windows:

cd build
cmake .. -G "Visual Studio 17 2022" -DCMAKE_TOOLCHAIN_FILE=conan_toolchain.cmake # depending of your vs version
cmake --build . --config Release
Release\${PROJECT_NAME}.exe

For others:
cd build
cmake .. -DCMAKE_TOOLCHAIN_FILE=conan_toolchain.cmake -DCMAKE_BUILD_TYPE=Release
cmake --build .
./${PROJECT_NAME}

https://docs.conan.io/2.0/tutorial/consuming_packages/build_simple_cmake_project.html